import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import api from '../api';
export default function Login(){const[f,setF]=useState({name:'',userType:'TENANT',password:''});const[msg,setMsg]=useState('');const nav=useNavigate();async function go(e){e.preventDefault();try{const r=await api.post('/auth/login',f);localStorage.setItem('user',JSON.stringify(r.data));nav(f.userType==='OWNER'?'/owner':'/tenant')}catch(e){setMsg(e.response?.data?.message||'Invalid credentials')}}return <div className="auth"><Link className="brand" to="/">stay<span>near</span></Link><div className="authgrid"><section className="intro"><small>WELCOME BACK</small><h1>Your next home is closer than you think.</h1><p>Sign in and discover rentals around your workplace.</p></section><form className="form" onSubmit={go}><h2>Welcome back</h2><p>Enter your details to continue.</p><label>Name<input required placeholder="Your name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Account type<select value={f.userType} onChange={e=>setF({...f,userType:e.target.value})}><option>TENANT</option><option>OWNER</option></select></label><label>Password<input required type="password" placeholder="••••••••" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><button>Sign in →</button>{msg&&<div className="msg">{msg}</div>}<p className="switch">New here? <Link to="/signup">Create account</Link></p></form></div></div>}